{
    "compilerOptions": {
        "baseUrl": ".",
        "paths": {
            // configure IntelliSense to recognize #root import paths in VSCode
            "#root/*.js": ["./*.js"]
        }
    }
}
